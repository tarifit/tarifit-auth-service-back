spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/tarifit_auth_test
  
  logging:
    level:
      com.tarifit.auth: DEBUG
      org.springframework.security: DEBUG
      org.springframework.data.mongodb: DEBUG

jwt:
  secret: test-secret-key-that-is-very-long-and-secure-for-testing-purposes-only
  expiration: 3600

management:
  endpoints:
    web:
      exposure:
        include: health,info
